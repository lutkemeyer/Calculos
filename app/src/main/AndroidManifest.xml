<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bordignon.calculos">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.bordignon.calculos.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.bordignon.calculos.CalcularActivity"
            android:label="@string/nome_tela_calcular"
            android:screenOrientation="portrait"/>
        <activity
            android:name="com.example.bordignon.calculos.EdicaoActivity"
            android:label="@string/nome_tela_edicao"
            android:screenOrientation="portrait"/>
    </application>

</manifest>